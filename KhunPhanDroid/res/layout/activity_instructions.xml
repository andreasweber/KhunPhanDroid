<RelativeLayout  xmlns:android="http://schemas.android.com/apk/res/android"
    android:layout_width="match_parent"
    android:layout_height="match_parent"
    android:paddingLeft="@dimen/activity_horizontal_margin"
    android:paddingRight="@dimen/activity_horizontal_margin"
    android:paddingTop="@dimen/activity_vertical_margin">

    <LinearLayout
        android:id="@+id/instructionListLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_above="@+id/instructionButtonLayout"
        android:layout_marginBottom="20dp"
        android:layout_alignParentTop="true"
        android:orientation="vertical">
    >
        <ExpandableListView
            android:id="@+id/listview_instructions"
            android:background="@drawable/instructions_list_shape"
            android:groupIndicator="@android:color/transparent"
            android:layout_width="match_parent"
            android:layout_height="wrap_content"
            />
    </LinearLayout>

    <LinearLayout
        android:id="@+id/instructionButtonLayout"
        android:layout_width="match_parent"
        android:layout_height="wrap_content"
        android:layout_marginBottom="30dp"
        android:layout_alignParentBottom="true"
        android:layout_alignParentRight="true"
        android:orientation="vertical">
    >
        <Button
            android:id="@+id/button_back"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:minHeight="0dp"
            android:layout_gravity="right|bottom"
            android:background="@drawable/button_shape"
            android:text="@string/button_back"
            android:textSize="@dimen/textsize_startbuttons"
            android:textColor="@drawable/button_selectable"
            android:textStyle="bold"
            android:onClick="onClickBack"
            />
    </LinearLayout>

</RelativeLayout >